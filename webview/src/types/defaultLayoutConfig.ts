/**
 * 默认布局配置
 */
import { LayoutConfig } from './LayoutConfig';

export { LayoutConfig };

export const defaultLayoutConfig: LayoutConfig = new LayoutConfig(
    150,
    50,
    80,
    30,
    "right"
);